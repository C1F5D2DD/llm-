# AutoXXT

学习通（超星 chaoxing）网页版自动化。

**网页版 + Edge 调试协议（CDP）**，全程后台：不移动真实鼠标、不需要窗口可见或获得焦点，
你可以一边挂着它一边打游戏、干别的。

## 文件

| 文件 | 作用 |
|---|---|
| `pageCapture.py` | 截图（CDP），图上叠坐标网格 |
| `window_control.py` | 输入（CDP）：点击 / 拖动 / 滚动 / 打字 / 按键，还能挪动浏览器窗口 |
| `brain.py` | 决策：把截图交给多模态模型，拿回一个动作 |
| `run.py` | 界面：左边实时画面 + 控制面板，右边对话区 |

## 快速开始

```bash
conda activate autoxxt
pip install -r requirements.txt
python run.py
```

`run.py` 会启动带调试端口的 Edge（登录态存在 `edge/`，只需登录一次）。

然后：左边填好 `接口地址 / 密钥 / 模型`，点「保存配置」，右边发一句话就行。
比如「帮我打开 Python 那门课」。

模型会自己连续跑：每一步的思考、动作、结果都会滚进对话历史，左边画面同步刷新。
跑的过程中你随时可以：

- **点停止** —— 当前这步执行完就停（网络请求没法打断，但它返回后不会再执行）
- **发消息插话** —— 排队带到下一步，不会丢
- **点清空** —— 清空对话历史和模型上下文

一个回合最多跑 50 步（`run.py` 里的 `MAX_STEPS`），防止模型停不下来。

## 配置模型

任何 **OpenAI 兼容**的接口都能用，三项配置在界面左侧填写。

**接口地址只填到版本号那一层，不要带 `/chat/completions`** ——OpenAI 库会自己接上，
多填就会拼成两遍（`…/v4/chat/completions/chat/completions`）导致 404，
而服务端只会回一句 "Not Found"，很难看出是自己填错了。
程序会自动去掉多余的这段并记一条警告，但还是填对最省事。

| 服务 | 接口地址 | 说明 |
|---|---|---|
| 智谱 BigModel | `https://open.bigmodel.cn/api/paas/v4` | `glm-4v-flash` 免费 |
| 深度求索 | `https://api.deepseek.com` | |
| 硅基流动 | `https://api.siliconflow.cn/v1` | |
| OpenRouter | `https://openrouter.ai/api/v1` | 免费模型每天 50 次 |

**必须选能看懂图的模型**，纯文本模型跑不起来（每步都要读截图）。
配置存在 `brain.config.json`，里面有密钥，已在 `.gitignore` 中排除。

两点实测经验：

- **推理模型能用，但费钱**。像 `glm-4.1v-thinking-flash` 这类会先输出一段
  `<think>…</think>` 再给答案，程序能正确解析（会自动剥掉思考段），但那段思考
  白花 token、还拖慢每步响应。能用非推理模型就别用推理模型。
- **截图很吃 token**。每步要发两张图（上一屏 + 当前屏），1080×720 的图按
  OpenAI 的视觉计费约 1100 token/张，一个 50 步的回合约 140k token 输入。
  这是"由模型自己判断动作有没有生效"的代价，详见下一节。

## 对话区怎么用

```
你：帮我打开 Python 那门课
AI  页面有课程卡片，点开 Python 语言程序设计
  ▸ 点击 (450, 320)
  ✓ 已点击 (450, 320)（地址变了 -> ...）
AI  进了课程页，点第一节
  ▸ 点击 (300, 180)
  ✓ 已点击 (330, 180)
AI  视频在播放，等一会儿
  ▸ 等待 30s
  ✓ 已等待 30s
■ 完成：已在第一节视频页
```

前缀含义：`AI`=模型的想法，`▸`=要执行的动作，`✓`=执行结果，`■`=回合结束，
`✗`=出错，`?`=模型求助，`·`=提示。

## 模型是怎么决定下一步的

每步把**两张截图**发给模型：上一屏 + 当前屏。**变没变由模型自己对比**——
程序不替它下"页面有没有变化"的结论。

这是踩坑换来的：程序能查的只有地址、滚动位置、标签页数这几项，而页面内部滚动、
跨域 iframe 里的滚动、弹窗浮现、按钮变灰它一概看不见。早先程序会据此断言
"页面没有任何变化"，把成功的动作报成失败，模型就反复重试同一个动作直到撞上限。
现在程序只报原始事实（`run.py` 的 `_facts()`），并明确告诉模型"以你看到的两张图为准"。

`page_info()` 会把页面大小、滚动位置等事实一起给模型，但它查不到时**会直说查不到**
（比如内容在跨域 iframe 里），绝不假装"页面只有一屏"。

## 坐标约定

截图上叠了一层半透明红色网格，数字就是那个位置的坐标（页面像素）。
模型读到多少就能填多少，喂给 `wc.click()` 就行——**不用做任何加减**。

网格是叠在图上而非画在边上：早先用刻度带，模型看图估坐标时总会多算一个带子的宽度，
实测偏 50 像素点不中。

## 作为库使用

```python
import pageCapture as cap
import window_control as wc
import brain

wc.init(pid)                 # 输入通道
cap.init(pid)                # 截图通道（内部起后台抓帧线程）
brain.init(base_url=..., api_key=..., model=...)

img = cap.get_frame()        # 最新一帧（带网格）
d = brain.decide(img, goal="进入我的课程列表", page=wc.page_info())
wc.click(d.action["x"], d.action["y"])
brain.note_result("已点击 (400, 300)")     # 把结果告诉模型，否则它会重复同一个动作

wc.type_text("要输入的内容")     # 在当前焦点输入（支持中文）
wc.press_key("Enter")         # 按键，支持组合键，比如 Control+a 全选
```

`cap.get_frame()` 几乎不耗时——后台线程一直在抓，它只是把现成的帧给你。
`cap.has_frame()` 可以先问有没有帧，避免等首帧时阻塞（界面循环该用这个）。

## 常见问题

**连不上 9222 端口？**
Edge 必须带 `--remote-debugging-port` 启动，并且要用**独立的 `--user-data-dir`**——
普通 Edge 已经在跑时，带参数再启动只会把标签页丢给旧实例然后退出，端口根本不开。
`run.py` 已经处理好这两点；手动启动是：

```bash
"C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" ^
  --remote-debugging-port=9222 --user-data-dir="edge" https://www.bilibili.com
```

**为什么用 CDP 而不是 Win32 截图？**
Win32 `PrintWindow` 快（15-56ms），但**要求窗口可见**——被遮挡或挪到屏幕外，
截出来是黑的或卡在旧帧。CDP 截图慢一些（40-70ms），但**不依赖窗口可见性**：
实测把窗口挪到屏幕外（-32000）之后，截图照常输出、内容也跟着页面变化更新，
点击也正常（0.11s）。所以「显示浏览器窗口」开关可以放心关掉——挂机打游戏时
把浏览器藏到屏幕外就行，模型和点击都不需要它在前台。

唯一要注意的是**滚动**：窗口藏起来后文档进入 hidden，滚轮事件有时等不到回执。
`window_control.scroll()` 处理了这件事——滚轮不通就直接改 DOM 的 `scrollTop`，
效果一样（返回值里会写明走了哪条路）。

## 依赖

`pillow`（图像处理）、`customtkinter`（界面）、`websockets`（CDP 通信）、`openai`（模型调用）

## 用途与风险

这个项目是**浏览器自动化的技术演示**，用来练习 CDP 截图与后台输入、多模态模型驱动
GUI 操作这些技术点。**请自觉遵守所在平台的服务条款和学校规定。**

需要明确的几点：

- **自动化操作学习通可能违反其服务条款**。用它刷课、刷时长，平台查得到（进度异常、
  操作轨迹异常），按作弊处理的话后果是账号封禁乃至学校处分，这个风险由使用者承担。
- 作者不对使用后果承担任何责任，也不提供针对特定平台的规避建议。
- 本项目按 MIT 协议开源（见下），但**许可证只约束代码的使用方式，不豁免平台条款**。
  拿着 MIT 授权的代码去做刷课生意，风险不会因为拿到了授权而消失。

## License

[MIT](LICENSE) © 2026 C1F5D2DD

